<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- INTERNET PERMISSION (Firebase, Image Upload, API calls) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- CAMERA PERMISSION (For fertility detection / live feed) -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- READ FILES (Needed for Image Picker, profile photo upload) -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <!-- WRITE FILES (Needed when saving images) -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <!-- REQUIRED FOR ANDROID 13+ (Scoped storage file access) -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <!-- Optional but recommended if you use foreground processing -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:label="EggGuardian"
        android:name="${applicationName}"
        android:icon="@mipmap/chick_icon">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

        </activity>

        <!-- Flutter Plugin Registrant (Do NOT remove) -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

    </application>

    <!-- Required for Flutter's Process Text plugin -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>

</manifest>
